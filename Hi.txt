A token is the basic unit of text an LLM reads and generates — not quite a word, not quite a character. It's a chunk of text (a word, part of a word, a punctuation mark, or even a space) that the model's tokenizer converts into a number, because neural networks operate on numbers, not raw text.
A token is the basic unit of text an LLM reads and generates — not quite a word, not quite a character. 
1.	Tokenization: Your input text is split into tokens using the tokenizer's vocabulary (typically 30,000–100,000+ possible tokens).